import { Helmet } from '@dr.pogodin/react-helmet';
import { Link } from 'react-router-dom';
import { useState } from 'react';
import { Grid2X2, Sparkles, Users, X } from 'lucide-react';
import { itinerary } from 'virtual:content';
import { cn } from '@/lib/utils';

const site = 'https://wander.app';

export default function ItineraryPage() {
  const [selectedFilter, setSelectedFilter] = useState('All');
  const [showAi, setShowAi] = useState(false);
  return <>
    <Helmet>
      <title>{itinerary.meta.title}</title>
      <meta name="description" content={itinerary.meta.description} />
      <link rel="canonical" href={`${site}/itinerary`} />
      <meta name="robots" content="noindex" />
    </Helmet>
    <main className="min-h-screen bg-background text-card-foreground">
      <header className="flex h-14 items-center justify-between border-b border-border bg-card px-4">
        <div className="flex items-center gap-7"><Link to="/" className="flex shrink-0 items-center text-xl font-bold tracking-tight text-accent" aria-label="Wander home">Wander</Link><nav className="hidden items-center gap-1 text-sm md:flex" aria-label="Workspace navigation"><Link to="/start-trip" className="rounded-lg px-3 py-2 hover:bg-muted">Trip Creation</Link><Link to="/start-trip?view=whiteboard#whiteboard" className="rounded-lg px-3 py-2 hover:bg-muted">Whiteboard</Link><Link to="/start-trip?view=whiteboard#trip-notifications" className="rounded-lg px-3 py-2 hover:bg-muted">Notifications</Link></nav></div>
        <div className="flex items-center gap-2"><button onClick={() => setShowAi((value) => !value)} className="rounded-lg p-2 text-muted-foreground hover:bg-muted" aria-label="Open AI suggestions"><Sparkles size={17} /></button><div className="flex -space-x-2">{['M', 'J', 'P', 'T'].map((letter, index) => <span key={letter} className={cn('flex h-7 w-7 items-center justify-center rounded-full border-2 border-card text-[10px] font-bold text-secondary-foreground', index === 0 ? 'bg-accent' : index === 1 ? 'bg-secondary' : 'bg-primary')}>{letter}</span>)}</div></div>
      </header>
      <section className="mx-auto max-w-4xl px-4 py-8 md:py-12">
        <div className="flex flex-col gap-4 border-b border-border pb-6 md:flex-row md:items-start md:justify-between"><div><h1 className="text-4xl font-bold tracking-tight" style={{ fontFamily: 'var(--font-heading)' }}>{itinerary.header.tripName}</h1><div className="mt-3 flex flex-wrap gap-x-4 gap-y-2 text-sm text-muted-foreground"><span>{itinerary.header.dates}</span><span className="flex items-center gap-1"><Users size={14} />{itinerary.header.travelers}</span><span>{itinerary.header.nights}</span></div><div className="mt-4 flex flex-wrap gap-2">{itinerary.header.tags.map((tag) => <span key={tag.id} className="rounded-full bg-muted px-3 py-1 text-xs font-medium text-muted-foreground">{tag.label}</span>)}</div></div><Link to="/start-trip?view=whiteboard#whiteboard" className="flex items-center gap-2 rounded-xl border border-border bg-card px-4 py-2 text-sm font-semibold hover:bg-muted"><Grid2X2 size={15} />{itinerary.header.board}<span className="rounded-full bg-accent px-1.5 text-[10px] text-accent-foreground">2</span></Link></div>
        <section className="mt-8"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-lg font-bold">{itinerary.cost.title}</h2><p className="mt-1 text-sm text-muted-foreground">Track the group total and each traveller&apos;s share.</p></div><div className="flex flex-wrap gap-1" role="group" aria-label="Cost category filters">{itinerary.cost.filters.map((filter) => <button key={filter.id} onClick={() => setSelectedFilter(filter.label)} className={cn('rounded-lg px-3 py-1.5 text-xs font-medium', selectedFilter === filter.label ? 'bg-card-foreground text-card' : 'bg-muted text-muted-foreground')}>{filter.label}</button>)}</div></div><div className="mt-4 grid grid-cols-1 gap-3 sm:grid-cols-2 lg:grid-cols-5">{itinerary.cost.cards.map((card, index) => <article key={card.id} className={cn('rounded-2xl border border-border bg-card p-4 shadow-sm', selectedFilter !== 'All' && selectedFilter !== card.label && 'hidden')}><p className="text-xs text-muted-foreground">{card.label}</p><p className={cn('mt-2 text-xl font-bold', index === 1 ? 'text-accent' : index === 2 ? 'text-secondary-foreground' : 'text-card-foreground')}>{card.value}</p><p className="mt-1 text-xs text-muted-foreground">{card.detail}</p></article>)}</div><div className="mt-5 grid grid-cols-1 gap-4 lg:grid-cols-[1.2fr_0.8fr]"><article className="rounded-2xl border border-border bg-card p-5 shadow-sm"><div className="flex items-center justify-between"><h3 className="font-bold">Category breakdown</h3><span className="text-sm text-muted-foreground">€1,700 total</span></div><div className="mt-5 space-y-4"><div><div className="flex items-center justify-between text-sm"><span>Flights</span><span className="font-semibold">€720 · 42%</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-muted"><div className="h-full w-[42%] rounded-full bg-accent" /></div></div><div><div className="flex items-center justify-between text-sm"><span>Lodging</span><span className="font-semibold">€540 · 32%</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-muted"><div className="h-full w-[32%] rounded-full bg-secondary" /></div></div><div><div className="flex items-center justify-between text-sm"><span>Food &amp; drinks</span><span className="font-semibold">€280 · 16%</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-muted"><div className="h-full w-[16%] rounded-full bg-primary" /></div></div><div><div className="flex items-center justify-between text-sm"><span>Activities</span><span className="font-semibold">€160 · 10%</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-muted"><div className="h-full w-[10%] rounded-full bg-card-foreground" /></div></div></div></article><article className="rounded-2xl border border-border bg-card p-5 shadow-sm"><h3 className="font-bold">Per-person split</h3><p className="mt-1 text-sm text-muted-foreground">4 travellers · equal share</p><p className="mt-5 text-3xl font-bold text-accent">€425</p><p className="mt-1 text-sm text-muted-foreground">estimated per traveller</p><div className="mt-5 border-t border-border pt-4 text-sm"><div className="flex justify-between"><span className="text-muted-foreground">Paid so far</span><span className="font-semibold">€1,260</span></div><div className="mt-3 flex justify-between"><span className="text-muted-foreground">Still to confirm</span><span className="font-semibold">€440</span></div></div></article></div></section>
        <section className="mt-10 space-y-8">{itinerary.days.map((day) => <div key={day.id}><h2 className="border-b border-border pb-3 text-sm font-bold">{day.date}</h2><div>{day.items.map((item) => <div key={item.id} className="grid grid-cols-[52px_minmax(0,1fr)_auto] items-center gap-3 border-b border-border py-4 text-sm"><time className="text-xs text-muted-foreground">{item.time}</time><div className="flex min-w-0 items-center gap-3"><span className={cn('h-2 w-2 shrink-0 rounded-full', item.status === 'confirmed' ? 'bg-accent' : 'bg-primary')} /><span className={cn('truncate font-medium', item.status === 'pending' && 'text-muted-foreground')}>{item.title}</span>{item.status === 'pending' && <span className="hidden rounded bg-muted px-1.5 py-0.5 text-[10px] text-muted-foreground sm:inline">pending</span>}</div><span className="text-xs text-muted-foreground">{item.cost}</span></div>)}</div></div>)}</section>
        <section className="mt-10 flex flex-col gap-4 rounded-2xl border border-border bg-card p-5 shadow-sm sm:flex-row sm:items-center sm:justify-between"><div><h2 className="font-bold">{itinerary.footer.title}</h2><p className="mt-1 text-sm text-muted-foreground">{itinerary.footer.description}</p></div><Link to="/start-trip?view=whiteboard#whiteboard" className="inline-flex items-center justify-center rounded-xl bg-accent px-4 py-2 text-sm font-semibold text-accent-foreground hover:bg-accent/90">{itinerary.footer.button}</Link></section>
      </section>
      {showAi && <aside className="fixed right-0 top-14 z-40 h-[calc(100vh-56px)] w-full max-w-sm border-l border-border bg-card p-5 shadow-2xl"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold uppercase tracking-wide text-muted-foreground">Wander AI</p><h2 className="mt-1 text-xl font-bold">Trip suggestions</h2></div><button onClick={() => setShowAi(false)} aria-label="Close AI suggestions"><X size={18} /></button></div><div className="mt-6 rounded-2xl border border-border bg-muted p-4"><div className="flex items-center gap-2 text-sm font-semibold"><Sparkles size={15} className="text-accent" />Route optimization</div><p className="mt-3 text-sm leading-6 text-muted-foreground">Group Belém Tower and MAAT Museum on Day 2 to reduce transit time.</p></div></aside>}
    </main>
  </>;
}
